from __future__ import annotations

import sys


def main() -> int:
    raise RuntimeError(
        "asset_bootstrap_worker.py has been retired. "
        "The living-room simulation no longer depends on external asset bootstrap."
    )


if __name__ == "__main__":
    raise SystemExit(main())
